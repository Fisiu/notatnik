---
title: "Starocie"
cascade:
  type: "legacy"          # Forces all sub-posts to use layouts/legacy/ single templates
  build:
    # list: never           # Prevents old posts from appearing in the main blog feed
    render: always        # Ensures the individual pages still exist
    section: legacy
---
